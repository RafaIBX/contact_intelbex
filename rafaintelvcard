<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="theme-color" content="#294CCC">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<title>Rafael Sánchez Torres — Intelbex</title>

<meta property="og:title" content="Rafael Sánchez Torres — Intelbex">
<meta property="og:description" content="Founder & Principal at Intelbex — Business strategy and digital execution.">
<meta property="og:type" content="profile">
<meta property="og:url" content="https://contact.intelbex.com">
<link rel="canonical" href="https://contact.intelbex.com">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

<style>
  :root {
    --ib-900: #0F1E4D;
    --ib-800: #1A2F73;
    --ib-700: #223F99;
    --ib-600: #294CCC;
    --ib-500: #4A6BE0;
    --ib-400: #6F8FF0;
    --ib-300: #9FB4F7;
    --ib-200: #C9D6FB;
    --ib-100: #E8EEFF;
    --ib-50:  #F5F8FF;
    --ink: #0B1020;
    --ink-soft: #4A5168;
    --ink-softer: #7A8099;
    --line: #E8EAF0;
    --bg: #F5F8FF;
    --card: #FFFFFF;
    --shadow-sm: 0 1px 2px rgba(15, 30, 77, 0.06), 0 1px 3px rgba(15, 30, 77, 0.04);
    --shadow-md: 0 4px 12px rgba(15, 30, 77, 0.08), 0 2px 6px rgba(15, 30, 77, 0.06);
    --shadow-lg: 0 20px 50px rgba(15, 30, 77, 0.15), 0 10px 20px rgba(15, 30, 77, 0.08);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

  html, body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: var(--bg);
    color: var(--ink);
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    overflow-x: hidden;
  }

  body {
    min-height: 100vh;
    min-height: 100dvh;
    padding: 16px;
    display: flex;
    justify-content: center;
    align-items: flex-start;
    background:
      radial-gradient(ellipse at top, rgba(41, 76, 204, 0.08) 0%, transparent 60%),
      var(--bg);
  }

  .card {
    width: 100%;
    max-width: 440px;
    background: var(--card);
    border-radius: 24px;
    box-shadow: var(--shadow-lg);
    overflow: hidden;
    position: relative;
    animation: fadeUp 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Top control bar */
  .top-bar {
    position: absolute;
    top: 16px;
    left: 16px;
    right: 16px;
    z-index: 10;
    display: flex;
    justify-content: space-between;
    align-items: center;
    pointer-events: none;
  }

  .top-bar > * { pointer-events: auto; }

  .edit-btn {
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: white;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: none;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s;
  }
  .edit-btn.unlocked { display: flex; }
  .edit-btn:hover { background: rgba(255, 255, 255, 0.3); }
  .edit-btn svg { width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  /* Passcode modal */
  .pin-modal {
    position: fixed;
    inset: 0;
    background: rgba(11, 16, 32, 0.7);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    display: none;
    justify-content: center;
    align-items: center;
    z-index: 1500;
    padding: 20px;
    opacity: 0;
    transition: opacity 0.2s;
  }
  .pin-modal.show { display: flex; opacity: 1; }

  .pin-box {
    background: white;
    border-radius: 20px;
    padding: 28px 24px 24px;
    width: 100%;
    max-width: 320px;
    text-align: center;
    box-shadow: var(--shadow-lg);
  }

  .pin-icon {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--ib-50);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--ib-600);
    margin: 0 auto 16px;
  }
  .pin-icon svg { width: 24px; height: 24px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .pin-title {
    font-size: 17px;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-bottom: 6px;
  }

  .pin-subtitle {
    font-size: 13px;
    color: var(--ink-soft);
    margin-bottom: 20px;
  }

  .pin-input {
    width: 100%;
    padding: 14px 16px;
    border: 1.5px solid var(--line);
    border-radius: 12px;
    font-family: inherit;
    font-size: 18px;
    font-weight: 600;
    text-align: center;
    letter-spacing: 0.3em;
    color: var(--ink);
    background: var(--ib-50);
  }
  .pin-input:focus {
    outline: none;
    border-color: var(--ib-600);
    background: white;
    box-shadow: 0 0 0 3px rgba(41, 76, 204, 0.1);
  }
  .pin-input.error {
    border-color: #DC2626;
    background: #FEF2F2;
    animation: shake 0.4s;
  }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-8px); }
    75% { transform: translateX(8px); }
  }

  .pin-error-text {
    font-size: 12px;
    color: #DC2626;
    margin-top: 8px;
    min-height: 16px;
    font-weight: 500;
  }

  .pin-actions {
    display: flex;
    gap: 8px;
    margin-top: 16px;
  }

  .pin-btn {
    flex: 1;
    padding: 12px;
    border-radius: 10px;
    font-family: inherit;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    border: none;
  }
  .pin-btn.primary { background: var(--ib-600); color: white; }
  .pin-btn.primary:hover { background: var(--ib-700); }
  .pin-btn.secondary { background: var(--ib-50); color: var(--ib-600); }

  .pin-setup-note {
    font-size: 11px;
    color: var(--ink-softer);
    margin-top: 14px;
    line-height: 1.4;
  }

  .lang-bar {
    display: flex;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 100px;
    padding: 3px;
    font-size: 12px;
    font-weight: 600;
  }
  .lang-bar button {
    background: transparent;
    border: none;
    color: rgba(255, 255, 255, 0.8);
    padding: 5px 12px;
    border-radius: 100px;
    cursor: pointer;
    font-family: inherit;
    font-size: 12px;
    font-weight: 600;
    transition: all 0.2s ease;
  }
  .lang-bar button.active {
    background: #FFFFFF;
    color: var(--ib-600);
  }

  /* Hero */
  .hero {
    background: linear-gradient(135deg, var(--ib-700) 0%, var(--ib-600) 50%, var(--ib-500) 100%);
    padding: 64px 24px 80px;
    text-align: center;
    position: relative;
    color: white;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -30%;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
    border-radius: 50%;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -50%;
    left: -30%;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.08) 0%, transparent 70%);
    border-radius: 50%;
  }

  .logo-mark {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 24px;
    font-weight: 700;
    font-size: 16px;
    letter-spacing: -0.01em;
    position: relative;
    z-index: 2;
  }

  .logo-box {
    width: 32px;
    height: 32px;
    background: white;
    border-radius: 7px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--ib-600);
    font-weight: 800;
    font-size: 15px;
    letter-spacing: -0.03em;
    overflow: hidden;
  }
  .logo-box img { width: 100%; height: 100%; object-fit: contain; }

  .photo-wrap {
    width: 128px;
    height: 128px;
    margin: 0 auto 20px;
    border-radius: 50%;
    padding: 4px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.4), rgba(255, 255, 255, 0.1));
    position: relative;
    z-index: 2;
    box-shadow: 0 10px 30px rgba(15, 30, 77, 0.3);
  }

  .photo {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    background: var(--ib-100);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--ib-600);
    font-size: 48px;
    font-weight: 700;
    overflow: hidden;
  }
  .photo img { width: 100%; height: 100%; border-radius: 50%; object-fit: cover; display: block; }

  .name {
    font-size: 26px;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 4px;
    position: relative;
    z-index: 2;
  }

  .title {
    font-size: 15px;
    font-weight: 500;
    opacity: 0.92;
    margin-bottom: 2px;
    position: relative;
    z-index: 2;
  }

  .company-line {
    font-size: 14px;
    font-weight: 400;
    opacity: 0.8;
    position: relative;
    z-index: 2;
  }

  .save-btn-wrap {
    margin-top: -36px;
    padding: 0 24px;
    position: relative;
    z-index: 5;
  }

  .save-btn {
    width: 100%;
    padding: 18px 24px;
    background: white;
    color: var(--ib-600);
    border: none;
    border-radius: 16px;
    font-family: inherit;
    font-size: 16px;
    font-weight: 600;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    cursor: pointer;
    box-shadow: var(--shadow-md);
    transition: all 0.2s ease;
    letter-spacing: -0.01em;
  }
  .save-btn:hover { transform: translateY(-2px); box-shadow: var(--shadow-lg); }
  .save-btn:active { transform: translateY(0); }
  .save-btn svg { width: 20px; height: 20px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .content { padding: 32px 20px 24px; }

  .section-label {
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--ink-softer);
    padding: 0 8px 10px;
  }
  .section-label:not(:first-child) { padding-top: 20px; }

  .row-list { background: var(--ib-50); border-radius: 16px; overflow: hidden; }

  .row {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    text-decoration: none;
    color: var(--ink);
    transition: background 0.15s ease;
    border-bottom: 1px solid rgba(41, 76, 204, 0.08);
    cursor: pointer;
  }
  .row:last-child { border-bottom: none; }
  .row:hover, .row:active { background: var(--ib-100); }

  .row-icon {
    width: 40px; height: 40px;
    border-radius: 10px;
    background: white;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    color: var(--ib-600);
    box-shadow: var(--shadow-sm);
  }
  .row-icon svg { width: 18px; height: 18px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .row-text { flex: 1; min-width: 0; }
  .row-label { font-size: 11px; font-weight: 500; color: var(--ink-softer); margin-bottom: 2px; }
  .row-value { font-size: 14px; font-weight: 500; color: var(--ink); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

  .row-chevron { color: var(--ink-softer); flex-shrink: 0; }
  .row-chevron svg { width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .actions-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin-top: 16px;
  }

  .action-btn {
    padding: 14px;
    background: white;
    border: 1.5px solid var(--line);
    border-radius: 14px;
    font-family: inherit;
    font-size: 13px;
    font-weight: 600;
    color: var(--ink);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    transition: all 0.2s ease;
  }
  .action-btn:hover, .action-btn:active { border-color: var(--ib-600); color: var(--ib-600); background: var(--ib-50); }
  .action-btn svg { width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .footer {
    padding: 24px 24px 28px;
    text-align: center;
    border-top: 1px solid var(--line);
    margin-top: 12px;
    background: var(--ib-50);
  }

  .footer-logo {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    font-weight: 600;
    color: var(--ink-soft);
    letter-spacing: -0.01em;
    margin-bottom: 6px;
  }

  .footer-logo-box {
    width: 20px; height: 20px;
    background: var(--ib-600);
    border-radius: 5px;
    display: flex; align-items: center; justify-content: center;
    color: white;
    font-weight: 800;
    font-size: 10px;
    letter-spacing: -0.03em;
    overflow: hidden;
  }
  .footer-logo-box img { width: 100%; height: 100%; object-fit: contain; }

  .footer-tagline { font-size: 11px; color: var(--ink-softer); }

  /* Toast */
  .toast {
    position: fixed;
    bottom: 32px;
    left: 50%;
    transform: translateX(-50%) translateY(100px);
    background: var(--ink);
    color: white;
    padding: 14px 24px;
    border-radius: 100px;
    font-size: 13px;
    font-weight: 500;
    box-shadow: var(--shadow-lg);
    opacity: 0;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 2000;
    pointer-events: none;
  }
  .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

  /* Edit Modal */
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(11, 16, 32, 0.6);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    display: none;
    justify-content: center;
    align-items: flex-end;
    z-index: 1000;
    padding: 0;
    opacity: 0;
    transition: opacity 0.2s;
  }
  .modal-overlay.show { display: flex; opacity: 1; }

  .modal {
    width: 100%;
    max-width: 480px;
    max-height: 92vh;
    max-height: 92dvh;
    background: white;
    border-radius: 24px 24px 0 0;
    display: flex;
    flex-direction: column;
    transform: translateY(100%);
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    overflow: hidden;
  }
  .modal-overlay.show .modal { transform: translateY(0); }

  @media (min-width: 500px) {
    .modal-overlay { align-items: center; padding: 20px; }
    .modal { border-radius: 24px; max-height: 85vh; }
  }

  .modal-header {
    padding: 20px 24px 16px;
    border-bottom: 1px solid var(--line);
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-shrink: 0;
  }

  .modal-title { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; }

  .modal-close {
    width: 32px; height: 32px;
    border: none;
    background: var(--ib-50);
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--ink-soft);
  }
  .modal-close svg { width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .modal-body {
    padding: 20px 24px;
    overflow-y: auto;
    flex: 1;
  }

  .form-group { margin-bottom: 16px; }
  .form-group:last-child { margin-bottom: 0; }

  .form-section {
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--ib-600);
    margin: 24px 0 12px;
    padding-bottom: 8px;
    border-bottom: 1px solid var(--line);
  }
  .form-section:first-child { margin-top: 0; }

  .form-label {
    display: block;
    font-size: 12px;
    font-weight: 600;
    color: var(--ink-soft);
    margin-bottom: 6px;
  }

  .form-input {
    width: 100%;
    padding: 12px 14px;
    border: 1.5px solid var(--line);
    border-radius: 10px;
    font-family: inherit;
    font-size: 14px;
    color: var(--ink);
    transition: border-color 0.15s;
    background: white;
  }
  .form-input:focus {
    outline: none;
    border-color: var(--ib-600);
    box-shadow: 0 0 0 3px rgba(41, 76, 204, 0.1);
  }

  .image-upload {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .image-preview {
    width: 64px;
    height: 64px;
    border-radius: 12px;
    background: var(--ib-50);
    border: 1.5px solid var(--line);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--ink-softer);
    overflow: hidden;
    flex-shrink: 0;
  }
  .image-preview.circle { border-radius: 50%; }
  .image-preview img { width: 100%; height: 100%; object-fit: cover; }
  .image-preview svg { width: 24px; height: 24px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .image-upload-actions { flex: 1; display: flex; gap: 8px; }

  .upload-btn {
    padding: 10px 14px;
    background: var(--ib-600);
    color: white;
    border: none;
    border-radius: 10px;
    font-family: inherit;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    flex: 1;
  }
  .upload-btn.secondary {
    background: var(--ib-50);
    color: var(--ib-600);
  }

  .modal-footer {
    padding: 16px 24px 20px;
    border-top: 1px solid var(--line);
    display: flex;
    gap: 10px;
    flex-shrink: 0;
    background: white;
  }

  .modal-btn {
    flex: 1;
    padding: 14px;
    border-radius: 12px;
    font-family: inherit;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    border: none;
    transition: all 0.15s;
  }
  .modal-btn.primary {
    background: var(--ib-600);
    color: white;
  }
  .modal-btn.primary:hover { background: var(--ib-700); }
  .modal-btn.secondary {
    background: var(--ib-50);
    color: var(--ib-600);
  }

  .export-row {
    display: flex;
    gap: 8px;
    margin-top: 8px;
  }

  .export-btn {
    flex: 1;
    padding: 10px;
    background: var(--ib-50);
    color: var(--ib-600);
    border: 1px solid var(--ib-100);
    border-radius: 10px;
    font-family: inherit;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
  }
  .export-btn svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

  .help-text {
    font-size: 11px;
    color: var(--ink-softer);
    margin-top: 6px;
    line-height: 1.4;
  }

  @media (max-width: 380px) {
    body { padding: 8px; }
    .hero { padding: 56px 20px 72px; }
    .name { font-size: 22px; }
    .photo-wrap { width: 112px; height: 112px; }
    .content { padding: 28px 16px 20px; }
  }
</style>
</head>
<body>

<div class="card">

  <!-- Top controls -->
  <div class="top-bar">
    <button class="edit-btn" id="edit-btn" onclick="requestEdit()" title="Edit" aria-label="Edit card">
      <svg viewBox="0 0 24 24"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
    </button>
    <div class="lang-bar">
      <button id="lang-en" class="active" onclick="setLang('en')">EN</button>
      <button id="lang-es" onclick="setLang('es')">ES</button>
    </div>
  </div>

  <!-- Hero -->
  <div class="hero">
    <div class="logo-mark">
      <div class="logo-box" id="logo-box"><span id="logo-text">ib</span></div>
      <span id="display-company">Intelbex</span>
    </div>

    <div class="photo-wrap">
      <div class="photo" id="photo-display">
        <span id="photo-initials">RS</span>
      </div>
    </div>

    <div class="name" id="display-name">Rafael Sánchez Torres</div>
    <div class="title" id="display-title">Founder &amp; Principal</div>
    <div class="company-line" id="display-org">Intelbex, Corp.</div>
  </div>

  <!-- Save to contacts -->
  <div class="save-btn-wrap">
    <button class="save-btn" onclick="saveContact()">
      <svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>
      <span id="save-label">Save to Contacts</span>
    </button>
  </div>

  <!-- Content -->
  <div class="content">

    <div class="section-label" id="label-contact">Contact</div>
    <div class="row-list">

      <a class="row" id="row-phone" href="#">
        <div class="row-icon"><svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg></div>
        <div class="row-text">
          <div class="row-label" id="label-phone">Phone</div>
          <div class="row-value" id="display-phone">—</div>
        </div>
        <div class="row-chevron"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></div>
      </a>

      <a class="row" id="row-email" href="#">
        <div class="row-icon"><svg viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg></div>
        <div class="row-text">
          <div class="row-label" id="label-email">Email</div>
          <div class="row-value" id="display-email">—</div>
        </div>
        <div class="row-chevron"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></div>
      </a>

      <a class="row" id="row-website" href="#" target="_blank" rel="noopener">
        <div class="row-icon"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg></div>
        <div class="row-text">
          <div class="row-label" id="label-website">Website</div>
          <div class="row-value" id="display-website">—</div>
        </div>
        <div class="row-chevron"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></div>
      </a>

      <a class="row" id="row-address" href="#" onclick="openMaps(event)">
        <div class="row-icon"><svg viewBox="0 0 24 24"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg></div>
        <div class="row-text">
          <div class="row-label" id="label-address">Address</div>
          <div class="row-value" id="display-address">—</div>
        </div>
        <div class="row-chevron"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></div>
      </a>

    </div>

    <div class="section-label" id="label-connect">Connect</div>
    <div class="row-list">

      <a class="row" id="row-li-personal" href="#" target="_blank" rel="noopener">
        <div class="row-icon"><svg viewBox="0 0 24 24" fill="currentColor" stroke="none"><path d="M20.5 2h-17A1.5 1.5 0 002 3.5v17A1.5 1.5 0 003.5 22h17a1.5 1.5 0 001.5-1.5v-17A1.5 1.5 0 0020.5 2zM8 19H5v-9h3zM6.5 8.25A1.75 1.75 0 118.3 6.5a1.78 1.78 0 01-1.8 1.75zM19 19h-3v-4.74c0-1.42-.6-1.93-1.38-1.93A1.74 1.74 0 0013 14.19a.66.66 0 000 .14V19h-3v-9h2.9v1.3a3.11 3.11 0 012.7-1.4c1.55 0 3.36.86 3.36 3.66z"/></svg></div>
        <div class="row-text">
          <div class="row-label" id="label-li-personal">LinkedIn · Personal</div>
          <div class="row-value" id="display-li-personal">—</div>
        </div>
        <div class="row-chevron"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></div>
      </a>

      <a class="row" id="row-li-company" href="#" target="_blank" rel="noopener">
        <div class="row-icon"><svg viewBox="0 0 24 24" fill="currentColor" stroke="none"><path d="M20.5 2h-17A1.5 1.5 0 002 3.5v17A1.5 1.5 0 003.5 22h17a1.5 1.5 0 001.5-1.5v-17A1.5 1.5 0 0020.5 2zM8 19H5v-9h3zM6.5 8.25A1.75 1.75 0 118.3 6.5a1.78 1.78 0 01-1.8 1.75zM19 19h-3v-4.74c0-1.42-.6-1.93-1.38-1.93A1.74 1.74 0 0013 14.19a.66.66 0 000 .14V19h-3v-9h2.9v1.3a3.11 3.11 0 012.7-1.4c1.55 0 3.36.86 3.36 3.66z"/></svg></div>
        <div class="row-text">
          <div class="row-label" id="label-li-company">LinkedIn · Company</div>
          <div class="row-value" id="display-li-company">—</div>
        </div>
        <div class="row-chevron"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></div>
      </a>

    </div>

    <div class="actions-grid">
      <button class="action-btn" onclick="shareCard()">
        <svg viewBox="0 0 24 24"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>
        <span id="label-share">Share</span>
      </button>
      <button class="action-btn" onclick="copyLink()">
        <svg viewBox="0 0 24 24"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
        <span id="label-copy">Copy Link</span>
      </button>
    </div>

  </div>

  <!-- Footer -->
  <div class="footer">
    <div class="footer-logo">
      <div class="footer-logo-box" id="footer-logo-box"><span id="footer-logo-text">ib</span></div>
      <span id="footer-company">Intelbex, Corp.</span>
    </div>
    <div class="footer-tagline" id="display-tagline">Business strategy &amp; digital execution</div>
  </div>

</div>

<!-- EDIT MODAL -->
<div class="modal-overlay" id="modal" onclick="handleOverlayClick(event)">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title" id="modal-title">Edit Card</div>
      <button class="modal-close" onclick="closeEditor()" aria-label="Close">
        <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
    </div>

    <div class="modal-body">

      <div class="form-section" id="sec-identity">Identity</div>

      <div class="form-group">
        <label class="form-label" id="lbl-photo">Photo</label>
        <div class="image-upload">
          <div class="image-preview circle" id="edit-photo-preview">
            <svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
          </div>
          <div class="image-upload-actions">
            <button class="upload-btn" onclick="document.getElementById('photo-input').click()" id="btn-upload-photo">Upload</button>
            <button class="upload-btn secondary" onclick="clearPhoto()" id="btn-remove-photo">Remove</button>
          </div>
        </div>
        <input type="file" id="photo-input" accept="image/*" style="display:none" onchange="handlePhotoUpload(event)">
        <div class="help-text" id="help-photo">Square image works best. Stored in your browser only.</div>
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-logo">Logo</label>
        <div class="image-upload">
          <div class="image-preview" id="edit-logo-preview">
            <span style="font-weight:800;font-size:18px;color:var(--ib-600);" id="edit-logo-initials">ib</span>
          </div>
          <div class="image-upload-actions">
            <button class="upload-btn" onclick="document.getElementById('logo-input').click()" id="btn-upload-logo">Upload</button>
            <button class="upload-btn secondary" onclick="clearLogo()" id="btn-remove-logo">Remove</button>
          </div>
        </div>
        <input type="file" id="logo-input" accept="image/*" style="display:none" onchange="handleLogoUpload(event)">
        <div class="help-text" id="help-logo">Transparent PNG works best.</div>
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-firstname">First Name</label>
        <input type="text" class="form-input" id="edit-firstName" placeholder="Rafa">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-lastname">Last Name</label>
        <input type="text" class="form-input" id="edit-lastName" placeholder="Sánchez">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-title">Position / Title</label>
        <input type="text" class="form-input" id="edit-title" placeholder="Founder & Principal">
      </div>

      <div class="form-section" id="sec-company">Company</div>

      <div class="form-group">
        <label class="form-label" id="lbl-org">Company Name</label>
        <input type="text" class="form-input" id="edit-org" placeholder="Intelbex, Corp.">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-tagline">Tagline</label>
        <input type="text" class="form-input" id="edit-tagline" placeholder="Business strategy & digital execution">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-loginitials">Logo Text Fallback</label>
        <input type="text" class="form-input" id="edit-logoText" placeholder="ib" maxlength="3">
        <div class="help-text" id="help-loginitials">Shown if no logo image is uploaded.</div>
      </div>

      <div class="form-section" id="sec-contact">Contact</div>

      <div class="form-group">
        <label class="form-label" id="lbl-phone">Phone</label>
        <input type="tel" class="form-input" id="edit-phone" placeholder="+1 (787) 123-4567">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-email">Email</label>
        <input type="email" class="form-input" id="edit-email" placeholder="you@company.com">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-website">Website</label>
        <input type="url" class="form-input" id="edit-website" placeholder="https://www.company.com">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-address">Street</label>
        <input type="text" class="form-input" id="edit-street" placeholder="123 Main St, Suite 100">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-city">City</label>
        <input type="text" class="form-input" id="edit-city" placeholder="San Juan">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-region">State / Region</label>
        <input type="text" class="form-input" id="edit-region" placeholder="PR">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-postal">Postal Code</label>
        <input type="text" class="form-input" id="edit-postal" placeholder="00901">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-country">Country</label>
        <input type="text" class="form-input" id="edit-country" placeholder="Puerto Rico">
      </div>

      <div class="form-section" id="sec-social">Social</div>

      <div class="form-group">
        <label class="form-label" id="lbl-lip">LinkedIn (Personal)</label>
        <input type="url" class="form-input" id="edit-linkedinPersonal" placeholder="https://linkedin.com/in/...">
      </div>

      <div class="form-group">
        <label class="form-label" id="lbl-lic">LinkedIn (Company)</label>
        <input type="url" class="form-input" id="edit-linkedinCompany" placeholder="https://linkedin.com/company/...">
      </div>

      <div class="form-section" id="sec-data">Data</div>

      <div class="form-group">
        <div class="export-row">
          <button class="export-btn" onclick="exportData()" id="btn-export">
            <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
            <span>Export</span>
          </button>
          <button class="export-btn" onclick="document.getElementById('import-input').click()" id="btn-import">
            <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
            <span>Import</span>
          </button>
          <button class="export-btn" onclick="resetData()" id="btn-reset">
            <svg viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/></svg>
            <span>Reset</span>
          </button>
        </div>
        <input type="file" id="import-input" accept="application/json,.json" style="display:none" onchange="importData(event)">
        <div class="help-text" id="help-data">Export saves a JSON backup. Import loads one.</div>

        <div class="export-row" style="margin-top: 10px;">
          <button class="export-btn" onclick="openPinModal('change'); closeEditor();" id="btn-change-pin">
            <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
            <span>Change PIN</span>
          </button>
          <button class="export-btn" onclick="closeEditor(); lockEditor();" id="btn-lock">
            <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
            <span>Lock</span>
          </button>
        </div>
      </div>

    </div>

    <div class="modal-footer">
      <button class="modal-btn secondary" onclick="closeEditor()" id="btn-cancel">Cancel</button>
      <button class="modal-btn primary" onclick="saveChanges()" id="btn-save">Save</button>
    </div>
  </div>
</div>

<!-- Passcode modal -->
<div class="pin-modal" id="pin-modal">
  <div class="pin-box">
    <div class="pin-icon">
      <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
    </div>
    <div class="pin-title" id="pin-title">Enter Passcode</div>
    <div class="pin-subtitle" id="pin-subtitle">Admin access required</div>
    <input type="password" inputmode="numeric" pattern="[0-9]*" class="pin-input" id="pin-input" maxlength="8" autocomplete="off">
    <div class="pin-error-text" id="pin-error"></div>
    <div class="pin-actions">
      <button class="pin-btn secondary" onclick="closePinModal()" id="pin-cancel">Cancel</button>
      <button class="pin-btn primary" onclick="submitPin()" id="pin-submit">Unlock</button>
    </div>
    <div class="pin-setup-note" id="pin-note"></div>
  </div>
</div>

<div class="toast" id="toast"></div>

<script>
  // ========= DEFAULT DATA =========
  const DEFAULT_DATA = {
    firstName: 'Rafael',
    lastName: 'Sánchez Torres',
    title: 'Founder & Principal',
    org: 'Intelbex, Corp.',
    tagline: 'Business strategy & digital execution',
    logoText: 'ib',
    phone: '+1 (939) 403-6095',
    email: 'rafael@intelbex.com',
    website: 'https://www.intelbex.com',
    street: '',
    city: 'San Juan',
    region: 'PR',
    postal: '',
    country: 'Puerto Rico',
    linkedinPersonal: '',
    linkedinCompany: 'https://www.linkedin.com/company/intelbex/',
    photoDataUrl: '',
    logoDataUrl: ''
  };

  const STORAGE_KEY = 'digitalCard.v1';
  const PIN_KEY = 'digitalCard.pinHash';
  const SESSION_KEY = 'digitalCard.unlocked';
  let data = { ...DEFAULT_DATA };
  let isUnlocked = false;
  let pinMode = 'verify'; // 'verify' | 'setup' | 'change'

  // ========= I18N =========
  const i18n = {
    en: {
      save: 'Save to Contacts',
      contact: 'Contact', connect: 'Connect',
      phone: 'Phone', email: 'Email', website: 'Website', address: 'Address',
      'li-personal': 'LinkedIn · Personal', 'li-company': 'LinkedIn · Company',
      share: 'Share', copy: 'Copy Link',
      savedToast: 'Contact file downloaded', copiedToast: 'Link copied',
      'modal-title': 'Edit Card',
      'sec-identity': 'Identity', 'sec-company': 'Company', 'sec-contact': 'Contact',
      'sec-social': 'Social', 'sec-data': 'Data',
      'lbl-photo': 'Photo', 'lbl-logo': 'Logo',
      'lbl-firstname': 'First Name', 'lbl-lastname': 'Last Name', 'lbl-title': 'Position / Title',
      'lbl-org': 'Company Name', 'lbl-tagline': 'Tagline', 'lbl-loginitials': 'Logo Text Fallback',
      'lbl-phone': 'Phone', 'lbl-email': 'Email', 'lbl-website': 'Website',
      'lbl-address': 'Street', 'lbl-city': 'City', 'lbl-region': 'State / Region',
      'lbl-postal': 'Postal Code', 'lbl-country': 'Country',
      'lbl-lip': 'LinkedIn (Personal)', 'lbl-lic': 'LinkedIn (Company)',
      'help-photo': 'Square image works best. Stored in your browser only.',
      'help-logo': 'Transparent PNG works best.',
      'help-loginitials': 'Shown if no logo image is uploaded.',
      'help-data': 'Export saves a JSON backup. Import loads one.',
      'btn-upload-photo': 'Upload', 'btn-remove-photo': 'Remove',
      'btn-upload-logo': 'Upload', 'btn-remove-logo': 'Remove',
      'btn-export': 'Export', 'btn-import': 'Import', 'btn-reset': 'Reset',
      'btn-cancel': 'Cancel', 'btn-save': 'Save',
      exportedToast: 'Backup downloaded', importedToast: 'Card imported',
      resetConfirm: 'Reset all card data to defaults?', resetToast: 'Card reset',
      saveToast: 'Card updated',
      pinEnter: 'Enter Passcode', pinSetup: 'Set Your Passcode', pinChange: 'Change Passcode',
      pinEnterSub: 'Admin access required',
      pinSetupSub: 'Create a 4–8 digit passcode to protect edit access',
      pinChangeSub: 'Enter a new 4–8 digit passcode',
      pinWrong: 'Incorrect passcode',
      pinTooShort: 'Passcode must be 4–8 digits',
      pinDigitsOnly: 'Digits only',
      pinUnlocked: 'Editor unlocked',
      pinSetupDone: 'Passcode set. Editor unlocked.',
      pinChanged: 'Passcode changed',
      pinLocked: 'Editor locked',
      pinCancel: 'Cancel', pinUnlock: 'Unlock', pinSet: 'Set',
      pinNoteSetup: 'Remember this. It protects your edit access on this browser.',
      pinNoteForgot: 'Forgot it? Triple-tap the photo to reset.',
      pinResetConfirm: 'Reset passcode? You will set a new one next time you edit.'
    },
    es: {
      save: 'Guardar en Contactos',
      contact: 'Contacto', connect: 'Conectar',
      phone: 'Teléfono', email: 'Correo', website: 'Sitio Web', address: 'Dirección',
      'li-personal': 'LinkedIn · Personal', 'li-company': 'LinkedIn · Empresa',
      share: 'Compartir', copy: 'Copiar Enlace',
      savedToast: 'Contacto descargado', copiedToast: 'Enlace copiado',
      'modal-title': 'Editar Tarjeta',
      'sec-identity': 'Identidad', 'sec-company': 'Empresa', 'sec-contact': 'Contacto',
      'sec-social': 'Social', 'sec-data': 'Datos',
      'lbl-photo': 'Foto', 'lbl-logo': 'Logotipo',
      'lbl-firstname': 'Nombre', 'lbl-lastname': 'Apellido', 'lbl-title': 'Cargo',
      'lbl-org': 'Empresa', 'lbl-tagline': 'Eslogan', 'lbl-loginitials': 'Texto del Logotipo',
      'lbl-phone': 'Teléfono', 'lbl-email': 'Correo', 'lbl-website': 'Sitio Web',
      'lbl-address': 'Dirección', 'lbl-city': 'Ciudad', 'lbl-region': 'Estado / Región',
      'lbl-postal': 'Código Postal', 'lbl-country': 'País',
      'lbl-lip': 'LinkedIn (Personal)', 'lbl-lic': 'LinkedIn (Empresa)',
      'help-photo': 'Imagen cuadrada. Se guarda solo en tu navegador.',
      'help-logo': 'PNG transparente recomendado.',
      'help-loginitials': 'Se muestra si no hay imagen de logo.',
      'help-data': 'Exportar guarda un respaldo JSON. Importar carga uno.',
      'btn-upload-photo': 'Subir', 'btn-remove-photo': 'Quitar',
      'btn-upload-logo': 'Subir', 'btn-remove-logo': 'Quitar',
      'btn-export': 'Exportar', 'btn-import': 'Importar', 'btn-reset': 'Restaurar',
      'btn-cancel': 'Cancelar', 'btn-save': 'Guardar',
      exportedToast: 'Respaldo descargado', importedToast: 'Tarjeta importada',
      resetConfirm: '¿Restaurar todos los datos?', resetToast: 'Tarjeta restaurada',
      saveToast: 'Tarjeta actualizada',
      pinEnter: 'Ingresar Código', pinSetup: 'Crear Código', pinChange: 'Cambiar Código',
      pinEnterSub: 'Se requiere acceso de administrador',
      pinSetupSub: 'Crea un código de 4 a 8 dígitos para proteger el acceso',
      pinChangeSub: 'Ingresa un nuevo código de 4 a 8 dígitos',
      pinWrong: 'Código incorrecto',
      pinTooShort: 'El código debe tener 4 a 8 dígitos',
      pinDigitsOnly: 'Solo dígitos',
      pinUnlocked: 'Editor desbloqueado',
      pinSetupDone: 'Código creado. Editor desbloqueado.',
      pinChanged: 'Código cambiado',
      pinLocked: 'Editor bloqueado',
      pinCancel: 'Cancelar', pinUnlock: 'Desbloquear', pinSet: 'Crear',
      pinNoteSetup: 'Recuerda este código. Protege el acceso de edición en este navegador.',
      pinNoteForgot: '¿Lo olvidaste? Toca la foto tres veces para reiniciar.',
      pinResetConfirm: '¿Reiniciar código? Crearás uno nuevo la próxima vez.'
    }
  };

  let currentLang = 'en';

  // ========= PASSCODE =========
  async function hashPin(pin) {
    // SHA-256 of PIN + static salt — sufficient for client-side gating
    const text = 'ibx.card.' + pin;
    const buf = new TextEncoder().encode(text);
    const hashBuf = await crypto.subtle.digest('SHA-256', buf);
    return Array.from(new Uint8Array(hashBuf)).map(b => b.toString(16).padStart(2, '0')).join('');
  }

  function getStoredPinHash() {
    try { return localStorage.getItem(PIN_KEY); } catch (e) { return null; }
  }

  function setStoredPinHash(hash) {
    try {
      if (hash === null) localStorage.removeItem(PIN_KEY);
      else localStorage.setItem(PIN_KEY, hash);
    } catch (e) { /* ignore */ }
  }

  function isSessionUnlocked() {
    try { return sessionStorage.getItem(SESSION_KEY) === '1'; } catch (e) { return false; }
  }

  function setSessionUnlocked(v) {
    try {
      if (v) sessionStorage.setItem(SESSION_KEY, '1');
      else sessionStorage.removeItem(SESSION_KEY);
    } catch (e) { /* ignore */ }
  }

  function openPinModal(mode) {
    pinMode = mode;
    const t = i18n[currentLang];
    const titleEl = document.getElementById('pin-title');
    const subEl = document.getElementById('pin-subtitle');
    const submitEl = document.getElementById('pin-submit');
    const noteEl = document.getElementById('pin-note');
    const input = document.getElementById('pin-input');
    const errEl = document.getElementById('pin-error');

    input.value = '';
    input.classList.remove('error');
    errEl.textContent = '';

    if (mode === 'setup') {
      titleEl.textContent = t.pinSetup;
      subEl.textContent = t.pinSetupSub;
      submitEl.textContent = t.pinSet;
      noteEl.textContent = t.pinNoteSetup;
    } else if (mode === 'change') {
      titleEl.textContent = t.pinChange;
      subEl.textContent = t.pinChangeSub;
      submitEl.textContent = t.pinSet;
      noteEl.textContent = t.pinNoteSetup;
    } else {
      titleEl.textContent = t.pinEnter;
      subEl.textContent = t.pinEnterSub;
      submitEl.textContent = t.pinUnlock;
      noteEl.textContent = t.pinNoteForgot;
    }
    document.getElementById('pin-cancel').textContent = t.pinCancel;

    document.getElementById('pin-modal').classList.add('show');
    setTimeout(() => input.focus(), 100);
  }

  function closePinModal() {
    document.getElementById('pin-modal').classList.remove('show');
  }

  async function submitPin() {
    const t = i18n[currentLang];
    const input = document.getElementById('pin-input');
    const errEl = document.getElementById('pin-error');
    const pin = input.value.trim();

    if (!/^\d+$/.test(pin)) {
      errEl.textContent = t.pinDigitsOnly;
      input.classList.add('error');
      return;
    }
    if (pin.length < 4 || pin.length > 8) {
      errEl.textContent = t.pinTooShort;
      input.classList.add('error');
      return;
    }

    const hash = await hashPin(pin);

    if (pinMode === 'setup' || pinMode === 'change') {
      setStoredPinHash(hash);
      isUnlocked = true;
      setSessionUnlocked(true);
      updateLockState();
      closePinModal();
      showToast(pinMode === 'setup' ? t.pinSetupDone : t.pinChanged);
      if (pinMode === 'setup') openEditor();
    } else {
      const stored = getStoredPinHash();
      if (hash === stored) {
        isUnlocked = true;
        setSessionUnlocked(true);
        updateLockState();
        closePinModal();
        showToast(t.pinUnlocked);
        openEditor();
      } else {
        errEl.textContent = t.pinWrong;
        input.classList.add('error');
        input.value = '';
      }
    }
  }

  function requestEdit() {
    if (isUnlocked) {
      openEditor();
      return;
    }
    const hasPin = getStoredPinHash();
    openPinModal(hasPin ? 'verify' : 'setup');
  }

  function updateLockState() {
    const btn = document.getElementById('edit-btn');
    btn.classList.toggle('unlocked', isUnlocked);
  }

  function lockEditor() {
    isUnlocked = false;
    setSessionUnlocked(false);
    updateLockState();
    showToast(i18n[currentLang].pinLocked);
  }

  function resetPin() {
    if (!confirm(i18n[currentLang].pinResetConfirm)) return;
    setStoredPinHash(null);
    isUnlocked = false;
    setSessionUnlocked(false);
    updateLockState();
    showToast(i18n[currentLang].pinLocked);
  }

  // Secret: triple-tap the photo to reset the PIN (recovery path)
  let photoTapCount = 0;
  let photoTapTimer = null;
  function handlePhotoTap() {
    photoTapCount++;
    clearTimeout(photoTapTimer);
    photoTapTimer = setTimeout(() => { photoTapCount = 0; }, 800);
    if (photoTapCount >= 3) {
      photoTapCount = 0;
      resetPin();
    }
  }

  // ========= STORAGE =========
  function loadData() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      if (raw) {
        data = { ...DEFAULT_DATA, ...JSON.parse(raw) };
      }
    } catch (e) { /* ignore — fall back to in-memory */ }
  }

  function persistData() {
    try {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
    } catch (e) { /* storage may be disabled — in-memory still works for session */ }
  }

  // ========= RENDERING =========
  function render() {
    // Identity
    document.getElementById('display-name').textContent = `${data.firstName} ${data.lastName}`.trim() || '—';
    document.getElementById('display-title').textContent = data.title || '';
    document.getElementById('display-org').textContent = data.org || '';
    document.getElementById('display-company').textContent = data.org ? data.org.replace(/,.*$/, '') : '';
    document.getElementById('footer-company').textContent = data.org || '';
    document.getElementById('display-tagline').textContent = data.tagline || '';

    // Photo
    const photoEl = document.getElementById('photo-display');
    const initials = ((data.firstName[0] || '') + (data.lastName[0] || '')).toUpperCase() || '•';
    if (data.photoDataUrl) {
      photoEl.innerHTML = `<img src="${escapeAttr(data.photoDataUrl)}" alt="">`;
    } else {
      photoEl.innerHTML = `<span>${escapeHtml(initials)}</span>`;
    }

    // Logo
    const logoEl = document.getElementById('logo-box');
    const footerLogoEl = document.getElementById('footer-logo-box');
    if (data.logoDataUrl) {
      logoEl.innerHTML = `<img src="${escapeAttr(data.logoDataUrl)}" alt="">`;
      footerLogoEl.innerHTML = `<img src="${escapeAttr(data.logoDataUrl)}" alt="">`;
    } else {
      logoEl.innerHTML = `<span>${escapeHtml(data.logoText || 'ib')}</span>`;
      footerLogoEl.innerHTML = `<span>${escapeHtml(data.logoText || 'ib')}</span>`;
    }

    // Phone
    setRow('phone', data.phone, data.phone ? `tel:${data.phone.replace(/[^+0-9]/g, '')}` : '#');

    // Email
    setRow('email', data.email, data.email ? `mailto:${data.email}` : '#');

    // Website
    const webDisplay = data.website ? data.website.replace(/^https?:\/\//, '').replace(/\/$/, '') : '';
    setRow('website', webDisplay, data.website || '#');

    // Address
    const addrParts = [data.street, data.city, data.region, data.postal, data.country].filter(Boolean);
    const addrDisplay = addrParts.join(', ') || '—';
    document.getElementById('display-address').textContent = addrDisplay;

    // LinkedIn
    const liPersonalDisplay = data.linkedinPersonal ? data.linkedinPersonal.replace(/^https?:\/\//, '').replace(/\/$/, '') : '';
    setRow('li-personal', liPersonalDisplay, data.linkedinPersonal || '#');
    const liCompanyDisplay = data.linkedinCompany ? data.linkedinCompany.replace(/^https?:\/\//, '').replace(/\/$/, '') : '';
    setRow('li-company', liCompanyDisplay, data.linkedinCompany || '#');

    // Apply i18n labels
    applyI18n();
  }

  function setRow(key, value, href) {
    const valEl = document.getElementById(`display-${key}`);
    if (valEl) valEl.textContent = value || '—';
    const rowEl = document.getElementById(`row-${key}`);
    if (rowEl) {
      rowEl.href = href;
      rowEl.style.opacity = value ? '1' : '0.5';
    }
  }

  function applyI18n() {
    const t = i18n[currentLang];
    document.getElementById('save-label').textContent = t.save;
    document.getElementById('label-contact').textContent = t.contact;
    document.getElementById('label-connect').textContent = t.connect;
    document.getElementById('label-phone').textContent = t.phone;
    document.getElementById('label-email').textContent = t.email;
    document.getElementById('label-website').textContent = t.website;
    document.getElementById('label-address').textContent = t.address;
    document.getElementById('label-li-personal').textContent = t['li-personal'];
    document.getElementById('label-li-company').textContent = t['li-company'];
    document.getElementById('label-share').textContent = t.share;
    document.getElementById('label-copy').textContent = t.copy;
    // Modal labels
    document.getElementById('modal-title').textContent = t['modal-title'];
    ['sec-identity','sec-company','sec-contact','sec-social','sec-data',
     'lbl-photo','lbl-logo','lbl-firstname','lbl-lastname','lbl-title','lbl-org','lbl-tagline','lbl-loginitials',
     'lbl-phone','lbl-email','lbl-website','lbl-address','lbl-city','lbl-region','lbl-postal','lbl-country',
     'lbl-lip','lbl-lic',
     'help-photo','help-logo','help-loginitials','help-data',
     'btn-upload-photo','btn-remove-photo','btn-upload-logo','btn-remove-logo',
     'btn-export','btn-import','btn-reset','btn-cancel','btn-save'
    ].forEach(id => {
      const el = document.getElementById(id);
      if (el && t[id]) {
        if (el.tagName === 'BUTTON') {
          const span = el.querySelector('span');
          if (span) span.textContent = t[id]; else el.textContent = t[id];
        } else {
          el.textContent = t[id];
        }
      }
    });
  }

  function setLang(lang) {
    currentLang = lang;
    document.documentElement.lang = lang;
    document.getElementById('lang-en').classList.toggle('active', lang === 'en');
    document.getElementById('lang-es').classList.toggle('active', lang === 'es');
    applyI18n();
  }

  // ========= EDITOR =========
  function openEditor() {
    // Populate form
    document.getElementById('edit-firstName').value = data.firstName || '';
    document.getElementById('edit-lastName').value = data.lastName || '';
    document.getElementById('edit-title').value = data.title || '';
    document.getElementById('edit-org').value = data.org || '';
    document.getElementById('edit-tagline').value = data.tagline || '';
    document.getElementById('edit-logoText').value = data.logoText || '';
    document.getElementById('edit-phone').value = data.phone || '';
    document.getElementById('edit-email').value = data.email || '';
    document.getElementById('edit-website').value = data.website || '';
    document.getElementById('edit-street').value = data.street || '';
    document.getElementById('edit-city').value = data.city || '';
    document.getElementById('edit-region').value = data.region || '';
    document.getElementById('edit-postal').value = data.postal || '';
    document.getElementById('edit-country').value = data.country || '';
    document.getElementById('edit-linkedinPersonal').value = data.linkedinPersonal || '';
    document.getElementById('edit-linkedinCompany').value = data.linkedinCompany || '';
    updatePhotoPreview();
    updateLogoPreview();
    document.getElementById('modal').classList.add('show');
    document.body.style.overflow = 'hidden';
  }

  function closeEditor() {
    document.getElementById('modal').classList.remove('show');
    document.body.style.overflow = '';
  }

  function handleOverlayClick(e) {
    if (e.target.id === 'modal') closeEditor();
  }

  function saveChanges() {
    data.firstName = document.getElementById('edit-firstName').value.trim();
    data.lastName = document.getElementById('edit-lastName').value.trim();
    data.title = document.getElementById('edit-title').value.trim();
    data.org = document.getElementById('edit-org').value.trim();
    data.tagline = document.getElementById('edit-tagline').value.trim();
    data.logoText = document.getElementById('edit-logoText').value.trim() || 'ib';
    data.phone = document.getElementById('edit-phone').value.trim();
    data.email = document.getElementById('edit-email').value.trim();
    data.website = normalizeUrl(document.getElementById('edit-website').value.trim());
    data.street = document.getElementById('edit-street').value.trim();
    data.city = document.getElementById('edit-city').value.trim();
    data.region = document.getElementById('edit-region').value.trim();
    data.postal = document.getElementById('edit-postal').value.trim();
    data.country = document.getElementById('edit-country').value.trim();
    data.linkedinPersonal = normalizeUrl(document.getElementById('edit-linkedinPersonal').value.trim());
    data.linkedinCompany = normalizeUrl(document.getElementById('edit-linkedinCompany').value.trim());
    persistData();
    render();
    closeEditor();
    showToast(i18n[currentLang].saveToast);
  }

  function normalizeUrl(url) {
    if (!url) return '';
    if (!/^https?:\/\//i.test(url)) return 'https://' + url;
    return url;
  }

  // ========= IMAGE UPLOAD =========
  function handlePhotoUpload(e) {
    const file = e.target.files[0];
    if (!file) return;
    resizeImage(file, 400, (dataUrl) => {
      data.photoDataUrl = dataUrl;
      updatePhotoPreview();
    });
  }

  function handleLogoUpload(e) {
    const file = e.target.files[0];
    if (!file) return;
    resizeImage(file, 256, (dataUrl) => {
      data.logoDataUrl = dataUrl;
      updateLogoPreview();
    });
  }

  function resizeImage(file, maxSize, callback) {
    const reader = new FileReader();
    reader.onload = (e) => {
      const img = new Image();
      img.onload = () => {
        const canvas = document.createElement('canvas');
        let w = img.width, h = img.height;
        if (w > h) { if (w > maxSize) { h = h * (maxSize / w); w = maxSize; } }
        else { if (h > maxSize) { w = w * (maxSize / h); h = maxSize; } }
        canvas.width = w; canvas.height = h;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0, w, h);
        // Preserve PNG transparency if source was PNG
        const type = file.type === 'image/png' ? 'image/png' : 'image/jpeg';
        callback(canvas.toDataURL(type, 0.9));
      };
      img.src = e.target.result;
    };
    reader.readAsDataURL(file);
  }

  function updatePhotoPreview() {
    const el = document.getElementById('edit-photo-preview');
    if (data.photoDataUrl) {
      el.innerHTML = `<img src="${escapeAttr(data.photoDataUrl)}" alt="">`;
    } else {
      el.innerHTML = `<svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>`;
    }
  }

  function updateLogoPreview() {
    const el = document.getElementById('edit-logo-preview');
    if (data.logoDataUrl) {
      el.innerHTML = `<img src="${escapeAttr(data.logoDataUrl)}" alt="">`;
    } else {
      el.innerHTML = `<span style="font-weight:800;font-size:18px;color:var(--ib-600);">${escapeHtml(data.logoText || 'ib')}</span>`;
    }
  }

  function clearPhoto() { data.photoDataUrl = ''; updatePhotoPreview(); }
  function clearLogo() { data.logoDataUrl = ''; updateLogoPreview(); }

  // ========= EXPORT / IMPORT / RESET =========
  function exportData() {
    const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'business-card-data.json';
    document.body.appendChild(a);
    a.click();
    setTimeout(() => { document.body.removeChild(a); URL.revokeObjectURL(url); }, 100);
    showToast(i18n[currentLang].exportedToast);
  }

  function importData(e) {
    const file = e.target.files[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = (ev) => {
      try {
        const parsed = JSON.parse(ev.target.result);
        data = { ...DEFAULT_DATA, ...parsed };
        persistData();
        render();
        openEditor();
        showToast(i18n[currentLang].importedToast);
      } catch (err) {
        showToast('Invalid file');
      }
    };
    reader.readAsText(file);
    e.target.value = '';
  }

  function resetData() {
    if (!confirm(i18n[currentLang].resetConfirm)) return;
    data = { ...DEFAULT_DATA };
    persistData();
    render();
    closeEditor();
    showToast(i18n[currentLang].resetToast);
  }

  // ========= vCARD =========
  function buildVCard() {
    const escape = (s) => (s || '').replace(/\\/g, '\\\\').replace(/;/g, '\\;').replace(/,/g, '\\,').replace(/\n/g, '\\n');
    const lines = [
      'BEGIN:VCARD',
      'VERSION:3.0',
      `N:${escape(data.lastName)};${escape(data.firstName)};;;`,
      `FN:${escape(`${data.firstName} ${data.lastName}`.trim())}`
    ];
    if (data.org) lines.push(`ORG:${escape(data.org)}`);
    if (data.title) lines.push(`TITLE:${escape(data.title)}`);
    if (data.phone) lines.push(`TEL;TYPE=CELL,VOICE:${data.phone}`);
    if (data.email) lines.push(`EMAIL;TYPE=WORK,INTERNET:${data.email}`);
    if (data.website) lines.push(`URL:${data.website}`);
    if (data.linkedinPersonal) lines.push(`URL;TYPE=LinkedIn:${data.linkedinPersonal}`);
    if (data.linkedinCompany) lines.push(`X-SOCIALPROFILE;TYPE=linkedin:${data.linkedinCompany}`);
    if (data.street || data.city || data.region || data.postal || data.country) {
      lines.push(`ADR;TYPE=WORK:;;${escape(data.street)};${escape(data.city)};${escape(data.region)};${escape(data.postal)};${escape(data.country)}`);
    }
    if (data.tagline) lines.push(`NOTE:${escape(data.tagline)}`);
    // Embed photo if present
    if (data.photoDataUrl && data.photoDataUrl.startsWith('data:image/')) {
      const match = data.photoDataUrl.match(/^data:image\/(\w+);base64,(.+)$/);
      if (match) {
        const type = match[1].toUpperCase();
        const b64 = match[2];
        // Fold long lines per vCard spec (75 chars)
        const folded = b64.match(/.{1,74}/g).join('\r\n ');
        lines.push(`PHOTO;ENCODING=b;TYPE=${type === 'JPG' ? 'JPEG' : type}:${folded}`);
      }
    }
    lines.push('END:VCARD');
    return lines.join('\r\n');
  }

  function saveContact() {
    const vcard = buildVCard();
    const blob = new Blob([vcard], { type: 'text/vcard;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `${data.firstName || 'contact'}_${data.lastName || 'card'}.vcf`;
    document.body.appendChild(a);
    a.click();
    setTimeout(() => { document.body.removeChild(a); URL.revokeObjectURL(url); }, 100);
    showToast(i18n[currentLang].savedToast);
  }

  function openMaps(e) {
    e.preventDefault();
    const parts = [data.street, data.city, data.region, data.postal, data.country].filter(Boolean);
    if (parts.length === 0) return;
    const query = encodeURIComponent(parts.join(', '));
    const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent);
    const url = isIOS
      ? `https://maps.apple.com/?q=${query}`
      : `https://www.google.com/maps/search/?api=1&query=${query}`;
    window.open(url, '_blank');
  }

  async function shareCard() {
    const shareData = {
      title: `${data.firstName} ${data.lastName} — ${data.org}`,
      text: `${data.firstName} ${data.lastName} · ${data.title}`,
      url: window.location.href
    };
    try {
      if (navigator.share) await navigator.share(shareData);
      else copyLink();
    } catch (err) { /* user cancelled */ }
  }

  function copyLink() {
    const url = window.location.href;
    if (navigator.clipboard) {
      navigator.clipboard.writeText(url).then(() => showToast(i18n[currentLang].copiedToast));
    } else {
      const t = document.createElement('textarea');
      t.value = url; document.body.appendChild(t); t.select();
      document.execCommand('copy'); document.body.removeChild(t);
      showToast(i18n[currentLang].copiedToast);
    }
  }

  function showToast(msg) {
    const toast = document.getElementById('toast');
    toast.textContent = msg;
    toast.classList.add('show');
    setTimeout(() => toast.classList.remove('show'), 2400);
  }

  function escapeHtml(s) { return String(s || '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
  function escapeAttr(s) { return String(s || '').replace(/"/g, '&quot;'); }

  // ========= INIT =========
  loadData();
  isUnlocked = isSessionUnlocked();
  render();
  updateLockState();

  // Triple-tap on photo to reset PIN (recovery path)
  document.getElementById('photo-display').addEventListener('click', handlePhotoTap);

  // Enter key submits the PIN
  document.getElementById('pin-input').addEventListener('keydown', (e) => {
    if (e.key === 'Enter') { e.preventDefault(); submitPin(); }
  });

  // Digits-only filter on PIN input
  document.getElementById('pin-input').addEventListener('input', (e) => {
    e.target.value = e.target.value.replace(/\D/g, '');
    e.target.classList.remove('error');
    document.getElementById('pin-error').textContent = '';
  });

  // Secret URL hash trigger: #edit opens the PIN prompt on load
  if (window.location.hash === '#edit') {
    setTimeout(() => requestEdit(), 300);
  }
</script>

</body>
</html>
